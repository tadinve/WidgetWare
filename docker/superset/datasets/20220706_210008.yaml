- columns:
  - column_name: region_id
    type: SMALLINT
  - column_name: region_description
    type: UNKNOWN
  metrics:
  - expression: COUNT(*)
    metric_name: count
    metric_type: count
    verbose_name: COUNT(*)
  schema: public
  table_name: region
